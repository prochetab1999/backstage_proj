import { apiDocsExtendedMyVersionPlugin } from './plugin';

describe('api-docs-extended-my-version', () => {
  it('should export plugin', () => {
    expect(apiDocsExtendedMyVersionPlugin).toBeDefined();
  });
});
